import { Component } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { FormBuilder, Validators } from '@angular/forms';

import { FormControl  } from '@angular/forms';

@Component({
  selector: 'app-borrar',
  templateUrl: './borrar.component.html',
  styleUrls: ['./borrar.component.scss']
})
export class BorrarComponent {
  
   /* myform: new FormGroup({});
  namex: new FormControl();

  constructor (private fg: FormGroup, private fc: FormControl) {
     this.namex = new FormControl('', [Validators.required, Validators.minLength(4)])    
    this.myform = new FormGroup({
      name : this.namex        
    }) 
  }
    

   ngOnInit() {
    console.log('oninit');
    this.myform.reset();
    // this.name = new FormControl('');
  } 

  send(): any {
    console.log('in send');
    //console.log(this.myform.errors);

  }    */
 

}
